import React from "react";
import styled from "styled-components";

const MessageListBlock = styled.div``;

function MessageList() {
  return <MessageListBlock></MessageListBlock>;
}

export default MessageList;
